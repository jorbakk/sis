PREFIX      = /opt/local
CFLAGS_GUI  = $(CFLAGS)
LDFLAGS_GUI = $(LDFLAGS) -framework Cocoa -framework OpenGL -framework IOKit -framework AppKit -framework QuartzCore
NFD_BACKEND = nfd_cocoa.m
SOK_BACKEND = sokol_app.m

